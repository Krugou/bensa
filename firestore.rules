rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Ultra-simple public read access for debugging
    match /{collection}/{document=**} {
      allow read: if true;
      allow write: if false; // Only Admin SDK (Scraper) can write
    }
  }
}
